{
  "name": "aletheia",
  "version": "0.0.0",
  "private": true,
  "description": "Privacy-preserving credential verification protocol. Verify the claim, not the document.",
  "license": "MIT",
  "packageManager": "pnpm@11.8.0",
  "engines": {
    "node": ">=22.6.0"
  },
  "scripts": {
    "build": "pnpm -r run build",
    "test": "pnpm -r run test",
    "typecheck": "pnpm -r run typecheck"
  }
}
